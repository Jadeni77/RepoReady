[project]
name = "python-basic"
version = "1.0.0"
description = "A deliberately bare Python project used to demo and test RepoReady."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
